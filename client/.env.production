# Loaded by `npm run build` (Netlify/Vercel builds). Variables set in the
# hosting dashboard override these values.
# VITE_API_URL must point at the deployed Express server, not a Netlify site.
VITE_API_URL=https://shopdoor.netlify.app/api/v1
VITE_DASHBOARD_URL=http://localhost:5174
